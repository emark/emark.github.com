# AGENTS.md - Developer Guidelines for emark.github.io

## Project Overview

This is a personal portfolio/resume website hosted on GitHub Pages. It is a static HTML site built with Bootstrap 5, with no build system, no tests, and no linting configuration.

## Project Structure

```
emark.github.io/
├── index.html          # Main HTML page
├── assets/
│   ├── js/color-modes.js      # Dark/light theme toggle
│   └── dist/
│       ├── css/bootstrap.min.css
│       └── js/bootstrap.bundle.min.js
├── media/profile.jpg
├── cert/               # PDF certificates
├── .opencode/          # OpenCode configuration
├── .github/workflows/  # GitHub Actions
└── AGENTS.md          # This file
```

## Build/Lint/Test Commands

This is a **static HTML site** - there is no build system, no test framework, and no linting configured.

### Local Development

Preview the site locally with any static file server:

```bash
# Using Python (Recommended)
python3 -m http.server 8000

# Using Node.js
npx serve .

# Using PHP
php -S localhost:8000
```

Then open http://localhost:8000 in your browser.

### Validation and Testing

No automated validation or tests are configured. For manual verification:
- W3C HTML Validator: https://validator.w3.org/
- Open the site locally and verify functionality
- Use browser developer tools to check for console errors

When making changes, manually verify: page loads without console errors, dark/light theme toggle works, all links are functional, site is responsive on mobile/tablet/desktop.

### CI/CD

The project uses OpenCode for AI-assisted code reviews via GitHub Actions:
- Workflow: `.github/workflows/opencode.yml`
- Triggered by `/oc` or `/opencode` commands in issues/PRs

## Code Style Guidelines

### HTML

- Use HTML5 semantic elements (`<header>`, `<main>`, `<footer>`, `<nav>`, etc.)
- Keep attributes in alphabetical order within tags
- Use double quotes for attribute values
- Indent with 2 spaces
- Self-closing tags should have a space before `/>` (e.g., `<br />`)
- Bootstrap classes are used throughout - follow Bootstrap conventions

### CSS

- Bootstrap CSS is loaded from `assets/dist/css/bootstrap.min.css` (minified, do not edit)
- Custom CSS is inline in `<style>` tags within `<head>`
- Follow Bootstrap's CSS class naming conventions
- Use existing Bootstrap utility classes before creating custom styles

### JavaScript

- JavaScript is in `assets/js/color-modes.js`
- Use strict mode (`'use strict'`)
- Use ES6+ syntax (const/let, arrow functions, template literals)
- Keep code minimal - only theme toggle functionality
- Use descriptive variable names (camelCase for variables, PascalCase for constants)
- Wrap code in IIFE to avoid global scope pollution
- Handle errors gracefully with try/catch for DOM manipulation
- Use console.warn instead of console.error for non-critical issues
- Always check if DOM elements exist before manipulating them (e.g., `if (!element) return`)
- Use template literals for string concatenation
- Prefer const over let; avoid var
- Use arrow functions for callbacks
- Chain array methods (forEach, map, filter) instead of traditional loops

### File Paths

- Use relative paths starting with `./` for same-directory references
- Example: `<script src="./assets/dist/js/bootstrap.bundle.min.js"></script>`
- Example: `<img src="./media/profile.jpg" />`

### Accessibility

- Always include `alt` attributes for images
- Use semantic HTML for proper screen reader support
- Include `aria-label` on interactive elements
- Ensure color contrast meets WCAG guidelines

### External Resources

The site uses these external CDNs:
- Bootstrap Icons: `https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css`
- Google Analytics (gtag.js)

Do not add new external dependencies without justification.

### Images

- Profile image: `media/profile.jpg` - circular, ~200x200px recommended
- Certificates: PDF format in `cert/` directory

### Versioning

- Version is displayed in the header (currently `v.1.0`)
- Update version number when making significant changes

## Common Tasks

### Adding a new certificate

1. Add the PDF file to the `cert/` directory
2. Add a new link in the Certificates section of `index.html`

### Updating profile information

Edit the relevant sections in `index.html`:
- About me: lines 229-241
- Education: accordion section
- Work experience: accordion section
- Links: in the Links section

### Changing theme colors

- Theme colors are defined in the `<style>` block in `<head>` (lines 31-104)
- Primary color: `#712cf9` (Bootstrap violet)
- To change, update `--bd-violet-bg` and related CSS variables

### Adding a new accordion section

Copy the existing accordion-item structure and:
1. Change `id="flush-collapseX"` to a unique ID
2. Update `data-bs-target` to match
3. Update `data-bs-parent` to point to the accordion parent

## Notes

- The HTML was originally generated by Astro v5.13.2 but is now manually maintained
- The site uses Bootstrap 5.3 examples starter template as a base
- Google Analytics tracking ID: G-SPHQQ2M88P
